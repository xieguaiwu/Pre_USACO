#include<bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
	int N;
	vector<int> will(N, 0);
	for (int i = 0; i < N; ++i) {
		cin >> will[i];
	}
    //计算每一个售价带来的顾客数量
    
	cin >> N;
}
