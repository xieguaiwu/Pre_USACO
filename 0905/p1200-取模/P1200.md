# P1200 [USACO1.1] 你的飞碟在这儿 (Your Ride Is Here)

> 来源: USACO Training Section 1.1 · 洛谷 https://www.luogu.com.cn/problem/P1200
> USACO 原题文件名: **ride.in / ride.out**

## 题目描述

众所周知，在每一个彗星后都有一只 UFO。这些 UFO 时常来收集地球上的忠诚支持者。不幸的是，他们的飞碟每次出行都只能带上一组支持者。因此，他们要用一种聪明的方案让这些小组提前知道谁会被彗星带走。他们为每个彗星起了一个名字，通过这些名字来决定这个小组是不是被带走的那个特定的小组。你的任务是写一个程序，通过小组名和彗星名来决定这个小组是否能被那颗彗星后面的 UFO 带走。

小组名和彗星名都以下列方式转换成一个数字：最终的数字就是名字中所有字母的积，其中 `A` 是 $1$，`Z` 是 $26$。

例如，`USACO` 小组就是 $21 \times 19 \times 1 \times 3 \times 15 = 17955$。

如果小组的数字 $\bmod\ 47$ 等于彗星的数字 $\bmod\ 47$，你就得告诉这个小组需要准备好被带走！（记住 "$a \bmod b$" 是 $a$ 除以 $b$ 的余数，例如 $34 \bmod 10$ 等于 $4$）

写出一个程序，读入彗星名和小组名并算出用上面的方案能否将两个名字搭配起来，如果能搭配，就输出 `GO`，否则输出 `STAY`。

## 输入格式

第 1 行：一个长度为 $1$ 到 $6$ 的大写字母串，表示**彗星**的名字。
第 2 行：一个长度为 $1$ 到 $6$ 的大写字母串，表示**队伍**的名字。

## 输出格式

一行内输出 `GO` 或 `STAY`。

## 样例

### 样例 1（samples/1.in / 1.out）

```
COMETQ
HVNGAT
```
```
GO
```

### 样例 2（samples/2.in / 2.out）

```
ABSTAR
USACO
```
```
STAY
```

## 说明/提示

- 小组名和彗星名均是没有空格或标点的一串大写字母（不超过 $6$ 个字母）。
- 题目翻译来自 NOCOW。

---

## 本地流水线提醒（做这题的真正目的）

1. `cp ../template.cpp p1200.cpp`
2. 取消注释 `freopen` 两行，改成 `"ride.in"` / `"ride.out"`
3. `../test.sh` → 两组样例全过
4. 注释回 freopen 两行 → 提交洛谷
5. commit：message 写明用时 + 卡点

⏱ **20 分钟封顶**。超时 = 工具/环境问题，不是算法问题，直接问。
