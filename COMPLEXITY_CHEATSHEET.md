# 复杂度速查表（USACO 向）

> 依据：usaco.org 官方规则 + USACO Guide（2026-08-02 整理）。

## 0. 三条铁律

1. 评测机 ≈ **10⁸ 次运算/秒**（常数好可达 5×10⁸）
2. C++ 每测试点 **2 秒**（≈ 2×10⁸ 预算）；Java/Python **4 秒**（但实际有效运算量低很多）
3. **看到 n ≥ 10⁵ → 默认必须 O(n log n) 或更好，O(n²) 直接放弃**

## 1. 时间/内存限制（USACO 官方）

| 语言 | 每测试点时限 | 内存 |
|---|---|---|
| C / C++ | 2 秒 | ~256 MB |
| Java | 4 秒 | ~256 MB |
| Python | 4 秒 | ~256 MB |

- 逐测试点计时：超时只丢该点分数，按过的测试点给分
- 个别比赛/题目会调整（如 4s/8s），以题面为准

## 2. 数据规模 → 可行复杂度（核心速看表）

| 数据规模 n | 可行复杂度 |
|---|---|
| ≤ 10 | O(n!), O(n⁷) |
| ≤ 20 | O(2ⁿ·n), O(n⁵) |
| ≤ 80 | O(n⁴) |
| ≤ 400 | O(n³) |
| ≤ 7500 | O(n²) |
| ≤ 7×10⁴ | O(n√n) |
| ≤ 5×10⁵ | **O(n log n)** |
| ≤ 5×10⁶ | O(n) |
| ≤ 10¹⁸ | O(log² n), O(log n), O(1) |

> 保守上限：常数差也能过；常数好可以再放宽一点，别指望太多。

## 3. 常见算法复杂度速查

| 算法 | 复杂度 |
|---|---|
| 排序（默认） | O(n log n) |
| 二分查找 | O(log n) |
| 枚举所有子集 | O(2ⁿ) |
| 枚举所有排列 | O(n!) |
| 枚举 k 元组 | O(nᵏ) |
| 素数判定/质因数分解（朴素） | O(√n) |
| 前缀和 | 预处理 O(n)，查询 O(1) |
| DFS/BFS | O(V+E) |
| Dijkstra | O((V+E) log V) |
| Floyd | O(V³) |
| 快速幂 | O(log n) |
| 区间 DP | O(n³)（可优化） |
| 背包（0/1） | O(n·W) |

## 4. 数据结构操作复杂度

| 结构 | 查询 | 修改 |
|---|---|---|
| 数组 | O(1) | O(1) |
| 排序集合/优先队列 | O(log n) | O(log n) |
| 树状数组（Fenwick） | O(log n) | O(log n) |
| 线段树 | O(log n) | O(log n) |
| ST 表 | O(1) | 不可改 |
| 哈希表 | O(1) 均摊 | O(1) 均摊 |

## 5. 常见陷阱

- **Java `Arrays.sort`（基本类型）最坏 O(n²)** → 用 `Collections.sort`（归并）
- Bronze 题 n ≤ 100 不给复杂度提示，正解也可能是 O(n)，别默认暴力
- 时限紧时"预期复杂度也可能 TLE"→ 注意常数因子（如有序→无序三元组遍历可快 6 倍）
- Python/Java 有双倍时间但**仍可能过不了** Gold/Platinum 的题；比赛用 C++ 最稳
- 数据量大时快读快写也是性能项（`scanf`/关同步 `cin.tie(0)`/`fread`）
