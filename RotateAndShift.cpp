#include<bits/stdc++.h>
using namespace std;

int total_pos, motivate, duration;
int current_pos[INT_MAX];

int main() {
	cin >> total_pos >> motivate >> duration;
	for (int i = 0; i < total_pos; ++i) {
		cin >> current_pos[i];
	}
}
