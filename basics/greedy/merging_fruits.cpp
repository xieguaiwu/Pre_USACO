#include<bits/stdc++.h>

using namespace std;

struct Chain {
    int weight;
    Chain *prev;
    Chain *next;
} Scheisse;

int minWeight(&int sum, &int newheap[]) {
    for (int i = 0; i < sum; ++i) {
        
    }
    return;
}

int main() {
	int sum;
	cin >> sum;
	int heap[sum];
	for (int i = 0; i < sum; ++i) {
		cin >> heap[i];
	}
    
}
